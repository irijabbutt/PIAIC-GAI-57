// 17- Write a script that concatenates two arrays [1, 2, 3] and [4, 5, 6] into a single array.
function concatinates(arr1:number[], arr2:number[])
{
 let result=console.log(`${arr1},`+`${arr2}`);
 return result
}
let arr3=[1,2,3];
let arr4 = [4, 5, 6];
concatinates(arr3,arr4)